const stringCounter = (stringA,stringB)=>{
    const addString = stringA + stringB
    return addString.length
}

module.exports = sum 













































































































